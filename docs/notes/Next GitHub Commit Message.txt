Next GitHub Commit Message

---------------------------------------------------------------------------------

Add automatic initial saving and scenario version actions

- Save each valid Event Inputs submission as a new Event
- Create an Original estimate Scenario before opening the workspace
- Persist complete scenario assumptions and ordered cost rows
- Add one Save action for workspace changes
- Default Save to creating a new Scenario
- Allow deliberate overwriting of the active Scenario
- Track unsaved changes and update the reset baseline after saving
- Prevent duplicate Scenario names within the same Event
- Protect all Event and Scenario writes with transactions
- Add persistence, validation, versioning, and rollback tests

---------------------------------------------------------------------------------

Complete saved event and scenario management

- Add a Saved Events screen grouped by Event and Scenario
- Open saved Scenarios directly in the interactive analysis workspace
- Recalculate results from persisted assumptions when loading
- Support Event and Scenario renaming with validation
- Support confirmed Scenario and Event deletion
- Prevent deleting an Event’s final Scenario by itself
- Protect unsaved workspace changes before navigation
- Preserve ordered labor and miscellaneous-cost entries
- Verify saved data, renames, deletions, and loading after restart
- Add saved-work, persistence, route, and workspace tests

---------------------------------------------------------------------------------

Build complete event comparison workflow

- Add selection for comparing two to four saved Scenarios
- Support comparisons within one Event or across different Events
- Recalculate every Scenario from its saved assumptions
- Add baseline selection and factual value differences
- Compare financial results, assumptions, targets, and warnings
- Highlight highest profit, lowest cost, and lowest break-even requirement
- Add Show differences only behavior
- Open any compared Scenario in the interactive workspace
- Add responsive, accessible comparison and error states
- Add comparison, route, persistence, and workspace tests

---------------------------------------------------------------------------------

Build Dashboard and first-use application flow

- Add the first-launch Welcome screen and four-step workflow
- Detect setup completion from saved Business Defaults
- Guide first-time users from Welcome to Defaults to Dashboard
- Open Dashboard directly on later launches
- Add Dashboard shortcuts for new analyses, saved events, comparison, and defaults
- Add useful event and comparison empty states
- Protect setup-required routes from direct access
- Preserve dirty-workspace navigation warnings
- Keep onboarding state consistent across application restarts
- Add onboarding, Dashboard, navigation, and route tests

---------------------------------------------------------------------------------

Add database versioning and safe migrations

- Establish the current application schema as Version 1
- Add a persistent schema migration ledger
- Create fresh databases through the migration system
- Safely adopt compatible existing unversioned databases
- Apply future migrations once and in numerical order
- Roll back failed migrations without partial schema changes
- Reject damaged or newer unsupported databases safely
- Protect existing customer records, identifiers, and relationships
- Add migration logging and developer documentation
- Add migration, startup, rollback, and data-preservation tests

---------------------------------------------------------------------------------

