Commit Milestones

FOUNDATION

☑ Initialize local application foundation
☑ Create shared application shell and navigation
☑ Document application architecture decisions
☑ Establish automated test structure
☑ Add application-wide error handling and logging


DEFAULTS

☑ Define business defaults data model
☑ Create business defaults database schema
☑ Build business defaults form
☑ Add defaults form validation
☑ Add model-level validation
☑ Save business defaults to the local database
☑ Load saved defaults into the Defaults screen
☑ Add confirmation and error states for saving defaults
☑ Verify first business defaults setup


EVENT CALCULATOR — INPUTS

☑ Define event analysis data model
☑ Update business defaults food and packaging cost method
☑ Create saved event database schema
☑ Build event identification form
☑ Build revenue assumptions section
☑ Build food and supply cost section
☑ Build labor cost section
  Current task; leave unchanged.

☑ Complete remaining event cost inputs
  Includes travel, parking, permits, utilities, vendor or booking fees,
  organizer commission, fixed processing fees, and named miscellaneous costs.

☑ Finish the complete Event Inputs workflow
  Apply saved defaults, allow independent event overrides, finish all input
  validation, add estimated-input indicators, and verify the complete form.


CALCULATION AND RESULTS

☑ Implement the core calculation engine
  Define terminology and calculate revenue, variable costs, fixed costs,
  total costs, owner pay, business profit, and profit margin. Include unit
  tests and manual examples.

☑ Extend the calculation engine with break-even analysis and factual warnings
  Use the existing calculation results to calculate break-even sales and
  customers, evaluate the selected profit target, and produce factual
  profitability and assumption warnings with focused tests.

☑ Revise the demand model for shared food demand and competition
  Replace the percentage expected to buy from this business with the
  percentage expected to buy food. Divide expected food demand among the
  user’s business and other competing food vendors, update revenue and
  order-based calculations, add early demand indicators and factual
  break-even comparisons, and update affected models, validation, and tests.

□ >Build the interactive Event Analysis workspace
  Create the primary post-input experience with financial results and
  adjustable scenario assumptions sharing the screen. Recalculate every
  dependent result and warning automatically as assumptions change.

□ *Add automatic initial saving and scenario version actions
  Automatically save each valid Event Inputs submission as a new Event with
  an “Original estimate” Scenario before opening the workspace. From the
  workspace, provide one Save action that defaults to saving adjustments as
  a new Scenario while allowing deliberate overwriting of the active Scenario.


SAVED WORK AND COMPARISON

□ *Complete saved event and scenario management
  List saved events and their scenarios, load any scenario directly into
  the interactive Event Analysis workspace, rename, delete, and verify
  persistence after restart.

□ Build the complete event comparison workflow
  Select scenarios, compare their financial results and assumptions,
  highlight meaningful differences, and open either scenario in the
  interactive workspace.


DASHBOARD AND ONBOARDING

□ Build the Dashboard and first-use application flow
  Add the Welcome screen, four-step introduction, setup detection,
  Welcome → Defaults → Dashboard flow, normal Dashboard launches,
  shortcuts, empty states, navigation safeguards, and session state.


DATABASE RELIABILITY

□ Add database versioning and safe migrations
  Track schema versions, run migrations once, support upgrades safely,
  and protect existing customer data.

□ Add backup, restore, and recovery safeguards
  Build backup and restore, test interrupted writes and recovery, and
  verify customer data stays separate from installed application files.


DESIGN AND ACCESSIBILITY

□ Complete V1 UI and UX design and polish
  Define the visual direction and reusable components, then polish Welcome,
  Dashboard, Defaults, Event Inputs, Results, Saved Events, Comparison,
  messages, empty states, wording, and visual hierarchy.

□ Complete accessibility and responsive behavior
  Add keyboard navigation, focus states, accessible labels and errors,
  contrast, responsive layouts, and screen-reader structure.


DESKTOP APPLICATION AND PACKAGING

□ Build the desktop application shell
  Add the dedicated window, automatic local-server startup, safe shutdown,
  single-instance protection, product identity, loading and error screens,
  and verified offline use.

□ Package the Windows application
  Bundle dependencies, add metadata and icons, create the installer and
  shortcuts, use the correct data location, and verify installation behavior.

□ Package the macOS application
  Bundle dependencies, add metadata and icons, create the installer or disk
  image, use the correct data location, and verify installation behavior.


QUALITY ASSURANCE

□ Add complete automated end-to-end workflow tests
  Cover onboarding, defaults and overrides, event analysis, saved scenarios,
  comparison, backup and restore, and application closure.

□ Complete platform acceptance testing and fix V1 defects
  Perform Windows and macOS acceptance testing and resolve all confirmed
  release-blocking defects.


DOCUMENTATION AND RELEASE

□ Finalize the complete V1 documentation set
  Finalize the PDR, ADR, calculation methodology, installation guide,
  usage guide, backup instructions, troubleshooting guide, README,
  release notes, and version history.

□ Prepare and publish Food Truck Event Profit Calculator V1
  Complete product and UI reviews, freeze calculation rules, prepare release
  candidates, verify clean installations, tag version 1.0.0, and publish.