Next Codex Prompt

---------------------------------------------------------------------------------

Implement the milestone:

Package the macOS application.

The desktop application shell, centralized application-data directory,
database migrations, backup and recovery safeguards, offline interface, and
complete customer workflows already exist.

Create a reproducible standalone macOS application bundle and drag-and-drop
disk image.

Supported platform

Target:

- macOS 13 Ventura or later
- Apple Silicon arm64
- Intel x86_64
- One Universal 2 application bundle and DMG

Use a Universal 2 Python installation and Universal 2-compatible native
dependencies.

Do not silently fall back to a single architecture.

If the available Python interpreter, PyObjC components, pywebview components,
or other required native dependency cannot produce a valid Universal 2 build:

- Stop the production build.
- Identify the incompatible component clearly.
- Do not label the artifact Universal 2.

Do not build:

- Mac App Store packages
- A PKG installer
- Separate Intel and Apple Silicon release packages
- Windows packages
- iOS applications

Application identity

Use:

Product name:
Food Truck Event Profit Calculator

Publisher:
The Fixer’s Desk

Application bundle:
Food Truck Event Profit Calculator.app

Executable name:
Food Truck Event Profit Calculator

Bundle identifier:
com.thefixersdesk.foodtruckeventprofitcalculator

DMG filename pattern:
FoodTruckEventProfitCalculator-{version}-universal.dmg

Define the application version in one centralized source of truth.

Reuse the version source already established for Windows packaging when
available.

Use that same version for:

- Application display
- CFBundleShortVersionString
- CFBundleVersion
- Build output names
- DMG name
- Documentation
- Release metadata

Do not introduce a second conflicting version value.

py2app application bundle

Use py2app to create the standalone macOS .app bundle from the existing
production desktop entrypoint.

Add py2app as the narrowly required macOS packaging dependency.

Create and check in a clear macOS packaging configuration.

Production builds must not use py2app alias or semi-standalone mode.

Bundle:

- The complete Python runtime needed by the application
- Flask and all runtime Python dependencies
- Required PyObjC components
- pywebview’s Cocoa and WebKit requirements
- Templates
- Static CSS and JavaScript
- Local startup-loading assets
- Local startup-error assets
- Required runtime documentation
- Application icon
- All other established local application resources

Do not bundle:

- Tests
- pytest caches
- Git metadata
- Development virtual environments
- Source-control configuration
- Local development databases
- data/app.db
- .ftbackup files
- Logs
- Recovery snapshots
- Restore staging files
- Customer records
- Secrets
- Signing credentials
- Developer-specific absolute paths

Ensure that bundled resource discovery works correctly during:

- Normal source execution
- Automated tests
- py2app frozen execution

Do not rely on the current working directory.

Use the existing resource-path abstraction or add a clear frozen-resource
abstraction shared by the desktop shell and packaging system.

Info.plist

Provide appropriate macOS application metadata, including:

- CFBundleIdentifier
- CFBundleName
- CFBundleDisplayName
- CFBundleExecutable
- CFBundleShortVersionString
- CFBundleVersion
- CFBundlePackageType
- CFBundleIconFile
- NSHumanReadableCopyright
- LSMinimumSystemVersion
- NSHighResolutionCapable

Configure the app as a normal foreground GUI application.

Do not register unsupported:

- Document types
- URL handlers
- Background services
- Login items
- System extensions
- Privileged helpers

Do not claim Mac App Store distribution or Apple certification.

Application icon

Use one checked-in .icns asset consistently for:

- Finder
- Dock
- Application bundle
- Get Info
- DMG presentation where appropriate

Prefer an approved final product icon if one exists.

If no approved icon exists:

- Add a clearly documented temporary local product icon.
- Do not download third-party artwork.
- Do not use copyrighted imagery.
- Make replacement straightforward before release.

Include the required icon sizes inside the .icns file and verify that macOS
recognizes the generated icon.

macOS application-data directory

Configure frozen macOS execution to use:

~/Library/Application Support/com.thefixersdesk.foodtruckeventprofitcalculator

Resolve all mutable data through the existing centralized application-data
system.

Store beneath it:

- Live SQLite database
- Logs
- Automatic recovery snapshots
- Backup and restore staging
- Webview browser-profile data
- Single-instance lock
- Shell temporary files
- Other mutable runtime state

Requirements:

- Create the directory safely on first launch.
- Do not write customer data inside the .app bundle.
- Do not write beside the application in /Applications.
- Do not write into bundled Resources.
- Do not depend on the application being writable.
- Preserve configurable development and testing data roots.
- Preserve isolated temporary roots in tests.
- Do not move or delete the current development data/app.db.

Installation and update model

Distribute the app through a drag-and-drop DMG.

The intended installation flow is:

1. Open the DMG.
2. Drag Food Truck Event Profit Calculator.app to Applications.
3. Eject the DMG.
4. Launch the application from Applications.

Support installation to either:

- /Applications
- The customer’s ~/Applications directory

Do not require administrator privileges from the application itself.

Updating the application consists of replacing the existing .app bundle with a
newer version.

Application replacement must not modify or remove customer data under
Application Support.

If the application is running during replacement:

- Document that it should be closed first.
- Do not implement a background updater.
- Do not modify active customer files from the DMG.

Reinstallation must reconnect to preserved customer data and open the
appropriate Welcome or Dashboard state.

Uninstallation consists of moving the .app to Trash.

Uninstallation must leave customer data intact, including:

- Business Defaults
- Events
- Scenarios
- Backups
- Recovery snapshots
- Logs

Do not add an automatic data-removal utility in this milestone.

DMG creation

Create a polished but restrained drag-and-drop DMG using macOS system tools.

The mounted DMG must contain:

- Food Truck Event Profit Calculator.app
- An Applications-folder alias or symbolic link

Use a clear volume name:

Food Truck Event Profit Calculator

Requirements:

- Preserve application-bundle symlinks and permissions.
- Use a compressed read-only distribution image.
- Do not include source code, build output, test data, or customer data.
- Do not rely on third-party DMG-building software unless already approved.
- Do not add decorative licensed backgrounds.
- Keep the DMG usable without internet access.

Create the DMG through reproducible scripted commands using tools available on
macOS, such as hdiutil and ditto.

Development and release build modes

Provide two explicit build modes.

Development mode:

- Build the complete standalone .app.
- Apply an appropriate ad-hoc signature where required.
- Create a development DMG when requested.
- Do not claim the output is Developer ID signed.
- Do not claim the output is notarized.
- Clearly label development artifacts and documentation accordingly.

Release mode:

- Require a valid Developer ID Application signing identity.
- Require the signing identity to be supplied through the build environment or
  keychain configuration.
- Sign all nested executable code correctly.
- Sign the outer application bundle last.
- Enable Hardened Runtime.
- Include a secure timestamp.
- Use only the minimum entitlements demonstrated to be required.
- Verify the application signature before producing the final DMG.
- Sign the DMG where appropriate.
- Submit the distribution artifact to Apple’s notary service using:
  xcrun notarytool
- Wait for the notarization result.
- Retrieve and preserve useful failure logs when notarization fails.
- Staple the successful notarization ticket.
- Verify the stapled ticket.
- Verify Gatekeeper acceptance.

Do not use altool.

Signing configuration

Read release signing configuration from secure build inputs.

Support an appropriate secure notarization-authentication method, such as an
existing notarytool keychain profile or approved App Store Connect API
credentials.

Do not:

- Commit certificates
- Commit private keys
- Commit Apple account passwords
- Commit app-specific passwords
- Commit notary credentials
- Print secrets in build logs
- Generate a fake Developer ID identity
- Use a self-signed identity while claiming a release build
- Proceed with release notarization when credentials are missing

When release signing is explicitly requested but configuration is unavailable:

- Fail clearly.
- Explain which category of configuration is missing.
- Preserve any valid unsigned development bundle separately.
- Do not rename the unsigned output as a release artifact.

Hardened Runtime and entitlements

Enable Hardened Runtime for Developer ID release builds.

Start with no optional runtime exceptions.

Add an entitlement only when the packaged application demonstrably requires
it to operate.

Document every included entitlement and why it is required.

Do not broadly add:

- Disable library validation
- Allow unsigned executable memory
- Allow JIT
- Debugging entitlement
- Network server entitlement intended for sandboxed applications
- File-access entitlements intended for sandboxed applications

unless testing proves the exact entitlement is necessary.

Do not enable App Sandbox in this direct-download V1 package.

Preserve localhost Flask operation and the existing offline design.

Code-signing verification

Verify the finished .app and DMG using appropriate macOS tools.

At minimum verify:

- The outer application signature
- Nested code signatures
- Hardened Runtime on release builds
- Bundle identifier
- Version metadata
- Architecture slices
- Gatekeeper assessment
- Notarization ticket
- DMG integrity

Fail the release build if any verification fails.

Build automation

Add a reproducible macOS build script using shell or Python orchestration.

The build should:

1. Verify it is running on macOS.
2. Verify the supported macOS build environment.
3. Verify Xcode Command Line Tools.
4. Verify the expected Python version.
5. Verify the Python interpreter supports the required Universal 2 build.
6. Verify all required native dependencies support arm64 and x86_64.
7. Verify py2app and required packaging-tool versions.
8. Verify the application icon.
9. Run focused macOS packaging tests.
10. Run the full automated test suite unless explicitly skipped for a
    documented developer-only reason.
11. Clean stale macOS packaging output.
12. Build the standalone .app.
13. Audit bundle contents.
14. Verify architectures and metadata.
15. Smoke-test the application bundle.
16. Apply development or release signing.
17. Create the DMG.
18. Sign, notarize, and staple in release mode.
19. Verify the final artifact.
20. Produce a SHA-256 checksum.
21. Place final outputs in a clear release-artifacts directory.

Do not automatically install or update:

- Python
- Xcode
- Xcode Command Line Tools
- py2app
- Apple certificates
- Signing credentials

Fail with clear instructions when a required build tool or input is missing.

Pin or record packaging-tool versions for reproducibility.

Build cleanliness

Clean only packaging-owned build and distribution directories.

Do not delete:

- Development databases
- Customer databases
- Backups
- Recovery snapshots
- Source files
- Documentation

Audit the .app and DMG for:

- Customer SQLite files
- .ftbackup files
- Logs
- Test fixtures
- pytest caches
- Git files
- Environment files
- Secrets
- Developer usernames
- Absolute development paths
- Unnecessary GUI frameworks
- Unrelated platform packaging files

Packaged application verification

Smoke-test the actual frozen .app rather than only source execution.

Verify:

- It launches by double-clicking without Terminal.
- It opens one native pywebview window.
- It starts the Flask loopback server automatically.
- It binds only to 127.0.0.1.
- It uses the expected product title and icon.
- It creates mutable data only under Application Support.
- It does not write into its own bundle.
- It performs fresh onboarding with an empty data root.
- It opens existing customer data.
- Live Event Analysis recalculation works.
- Scenario persistence works.
- Saved Events and comparison work.
- Backup download works.
- Restore validation works.
- Single-instance protection works.
- Closing the window stops the local server.
- It operates with external network access blocked.
- It remains functional after being copied into Applications.
- It remains functional after application replacement.

macOS installation acceptance

Verify in clean macOS environments where practical.

Clean installation:

- DMG mounts successfully.
- The application can be copied to Applications.
- Application identity and icon display correctly.
- First launch creates the correct Application Support directory.
- Onboarding works.
- No customer data is created inside the app bundle.

Apple Silicon:

- The app launches natively as arm64.
- It does not require Rosetta.
- All packaged native dependencies contain a compatible arm64 slice.

Intel:

- The app launches natively as x86_64.
- All packaged native dependencies contain a compatible x86_64 slice.

Existing data:

- Defaults, Events, Scenarios, and backups remain available after replacing
  the app.

Upgrade:

- Replacing an older app bundle updates installed application resources.
- Customer data remains unchanged.
- Required schema migrations run through the existing safe migration system.
- The application does not create a second customer-data directory.

Uninstall and reinstall:

- Moving the app to Trash removes the application.
- Application Support data remains.
- Reinstalling the app finds and uses preserved data.

Offline:

- Installation from the DMG requires no internet access.
- Normal application workflows require no internet.
- A notarization ticket stapled to a release DMG remains verifiable offline
  where macOS permits.

Failure states:

- A damaged database opens the established recovery state.
- Missing signing credentials block only release signing and notarization.
- A failed notarization never produces an artifact labeled as release-ready.
- Invalid architecture slices fail production verification.
- Build failure does not alter development or customer data.

Testing

Add focused automated tests where behavior does not require a physical macOS
desktop, including:

- macOS frozen-resource resolution
- macOS Application Support data-root selection
- Bundle identifier and version metadata
- Info.plist generation
- DMG artifact naming
- Universal 2 build configuration
- Icon configuration
- Exclusion of customer-data files
- Development versus release modes
- Missing-signing configuration
- Notarization-command construction without exposing credentials
- Bundle-content auditing
- Build-script validation
- Importing packaging modules without side effects
- Existing non-macOS tests remaining runnable

Use scripted and manual macOS acceptance checks for behaviors that require
Finder, Gatekeeper, native windowing, code signing, notarization, or different
Mac architectures.

Documentation

Add concise developer documentation explaining:

- Supported macOS versions and architectures
- Required macOS build machine
- Required Python and py2app setup
- Bundle identifier
- Application Support data location
- How to create a development build
- How to create a release build
- Developer ID requirements
- Hardened Runtime and entitlements
- notarytool authentication
- Notarization and stapling
- DMG creation
- Generated artifact locations
- Installation and replacement behavior
- Uninstall data preservation
- Apple Silicon acceptance testing
- Intel acceptance testing
- Expected Gatekeeper behavior for unsigned development builds
- Release-readiness requirements

Scope

Do not implement:

- Mac App Store distribution
- PKG installer distribution
- Automatic updates
- Sparkle or another update framework
- Apple receipt validation
- License keys
- Cloud services
- Telemetry
- Crash-report uploading
- Destructive uninstall
- Final V1 publication

Do not change calculations, business behavior, persistence rules, or customer
workflows.

Do not make unrelated changes.
Do not commit.

Afterward:

1. Run focused macOS packaging, frozen-runtime, metadata, and data-path tests.
2. Run the full automated test suite.
3. Run git diff --check.
4. Build the Universal 2 application when a suitable macOS environment is
   available.
5. Build the DMG when macOS packaging tools are available.
6. Sign and notarize only when valid release credentials are available.
7. Report exact generated artifact paths and SHA-256 checksums.
8. Summarize installation, replacement, uninstall, and data-preservation
   behavior.
9. List each Apple Silicon and Intel acceptance check completed.
10. Clearly report any unavailable Mac hardware, build tools, Developer ID
    identity, notarization credentials, or clean test environment.

Keep the response concise.

---------------------------------------------------------------------------------

Implement the milestone:

Add complete automated end-to-end workflow tests.

The full V1 application workflows, desktop shell, Windows packaging,
macOS packaging, calculations, persistence, migrations, backup and restore,
responsive design, and accessibility behavior already exist.

Add automated browser-level and desktop-lifecycle tests proving that the
customer’s critical workflows operate correctly when the complete application
is assembled.

Testing technology

Use the official Playwright pytest plugin for browser end-to-end tests.

Add only the narrowly required development and testing dependencies.

Use:

- Chromium for the complete end-to-end suite
- Firefox and WebKit for a smaller critical-workflow smoke suite

Document the Playwright browser-installation command required after installing
Python dependencies.

Do not require every developer test run to execute the full suite in all three
browser engines.

Do not introduce Selenium, Cypress, or a second browser-automation framework.

Test architecture

Create a clearly separated end-to-end test area, such as:

tests/e2e/

Keep end-to-end tests distinct from:

- Unit tests
- Domain tests
- Repository tests
- Route tests
- Packaging tests
- Manual platform-acceptance checks

Provide reusable pytest fixtures for:

- Isolated temporary application-data directories
- Real temporary SQLite databases
- Database migration and startup
- Real Flask servers bound to 127.0.0.1
- Operating-system-selected available ports
- Browser base URLs
- Clean browser contexts
- Download directories
- Restarting the application against the same temporary customer data
- Starting with deliberately prepared saved data when a workflow genuinely
  requires it
- Cleaning all server threads, files, and processes after tests

Every browser test must use an isolated application-data root unless a
specific restart test deliberately reuses its own temporary root.

Never read, modify, copy over, migrate, back up, or restore the real:

data/app.db

Do not use customer or developer backup files as fixtures.

Real-system boundary

Browser end-to-end tests must exercise:

- Real Flask routes
- Real templates
- Real CSS and JavaScript
- Real form validation
- Real calculation logic
- Real SQLite persistence
- Real database migrations
- Real backup generation
- Real restore validation
- Real session behavior
- Real navigation safeguards

Do not mock:

- Calculation results
- Database repositories
- Save behavior
- Scenario loading
- Comparison calculations
- Backup contents
- Restore replacement
- Setup-completion detection

Mock only genuinely external or native boundaries where unavoidable, such as:

- The pywebview native-window adapter in headless lifecycle tests
- Operating-system file dialogs not used by the browser interface
- Platform signing or installer behavior, which is outside this milestone

Customer-oriented locators

Prefer Playwright locators based on:

- Accessible role
- Visible label
- Accessible name
- Visible text where appropriate

Use test IDs only when no stable customer-visible or semantic locator exists.

Do not rely on:

- XPath
- Deep CSS selectors
- Element position
- Generated framework classes
- Database identifiers exposed only for tests
- Exact incidental whitespace
- Exact timestamps unless time is explicitly controlled

The tests should reinforce the accessibility work rather than bypass it.

Waiting and reliability

Do not use arbitrary sleep calls.

Use:

- Playwright’s normal actionability waiting
- Auto-retrying assertions
- Explicit waiting for meaningful customer-visible states
- Expected downloads
- Relevant route or calculation responses where necessary
- Application-provided status regions

For live Event Analysis:

- Wait for the updating state when useful.
- Wait for the completed analysis status or updated displayed value.
- Ensure an older response does not replace a newer edit.
- Do not depend on a hardcoded debounce duration.

Configure sensible test-level timeouts without hiding genuine hangs behind
excessively long global timeouts.

Test-data strategy

Create concise builders or helpers for entering predictable customer data.

Use realistic but clearly fictional values.

Maintain at least:

1. One standard profitable Event
2. One weaker alternate Scenario
3. One loss-producing Scenario
4. One Event suitable for comparison across Events
5. Business Defaults containing employee labor and a profit target
6. A Scenario containing multiple labor rows and miscellaneous costs

Avoid scattering large duplicated field-entry blocks across every test.

Do not build an elaborate page-object framework.

Use small focused helpers for repeated customer actions, such as:

- Complete first-time Defaults
- Enter a standard Event
- Wait for live analysis completion
- Save as a new Scenario
- Open Saved Events
- Select Scenarios for comparison
- Download a backup
- Upload and confirm a restore

Helpers must still operate through the visible UI.

Complete customer-journey test

Add one complete browser journey that begins with a fresh application-data
directory and proves the following sequence:

1. Launch the fresh application.
2. Verify the Welcome screen appears.
3. Review the four-step introduction.
4. Select Get started.
5. Enter and save complete Business Defaults.
6. Verify navigation to Dashboard.
7. Start a new Event analysis.
8. Verify applicable Defaults are prefilled.
9. Enter the complete Event-specific information.
10. Submit Event Inputs.
11. Verify:
    - The Event is automatically saved
    - The Original estimate Scenario is created
    - The interactive workspace opens
    - Initial results are displayed
12. Change multiple workspace assumptions, including:
    - Attendance
    - Weather or event protection
    - Percentage expected to buy food
    - Competing food vendors
    - One revenue or cost assumption
    - Employee labor
13. Verify dependent revenue, cost, profit, margin, break-even, target, and
    warning output updates without a Calculate button.
14. Save the adjusted state as a new named Scenario.
15. Modify it again and overwrite the active Scenario with explicit
    confirmation.
16. Open Saved Events.
17. Verify the Event and both Scenarios appear.
18. Open each Scenario and verify its saved assumptions and recalculated
    results differ as expected.
19. Compare the two Scenarios.
20. Change the comparison baseline.
21. Enable Show differences only.
22. Open one Scenario from comparison.
23. Download a customer backup.
24. Make a visible persisted change after the backup, such as renaming a
    Scenario or creating another Scenario.
25. Restore the downloaded backup through the customer interface.
26. Confirm complete replacement.
27. Verify the post-backup change is absent.
28. Verify the backed-up Defaults, Event, Scenarios, and ordered child rows
    remain.
29. Restart the Flask application using the same temporary data root.
30. Verify launch opens Dashboard and all restored data remains usable.

This journey must not inspect or manipulate the database directly to prove
customer-visible outcomes.

Focused onboarding and Defaults workflow

Add browser tests covering:

- Fresh launch opening Welcome
- Get started opening Defaults
- Invalid Defaults remaining on the form
- Error summary and field errors appearing
- Entered values remaining intact
- Successful setup opening Dashboard
- Restart after setup opening Dashboard
- Existing Defaults preventing repeat onboarding
- Updating Defaults without altering saved Event assumptions
- Analyze new event using the latest Defaults

Focused Event Inputs and workspace workflow

Add browser tests covering:

- Complete Event Inputs submission
- Automatic Event and Original estimate persistence
- Invalid submission creating no visible saved Event
- Defaults indicators and event-specific override indicators
- Shared food-demand indicator
- Live recalculation after meaningful changes
- Adding and removing labor rows
- Adding and removing miscellaneous-cost rows
- Custom expected-sales behavior
- Invalid live edits preserving the latest valid results
- Correcting the invalid field updating results
- Reset changes restoring the active saved baseline
- Start new analysis opening a clean form
- Dirty-workspace navigation confirmation
- Canceling navigation preserving current edits
- Confirming navigation leaving the saved Scenario unchanged

Focused Scenario version workflow

Add browser tests covering:

- Original estimate being created automatically
- Save being disabled when the workspace is clean
- Save as new Scenario being selected by default
- Blank Scenario-name validation
- Duplicate-name validation
- Case-insensitive duplicate detection
- Save as new preserving the original Scenario
- New Scenario becoming active
- Overwrite requiring explicit confirmation
- Overwrite changing only the active Scenario
- Reset baseline updating after a successful save
- Failed save preserving workspace values and results

Focused Saved Events workflow

Add browser tests covering:

- Empty Saved Events state
- Events grouped with their Scenarios
- Opening a saved Scenario
- Loaded workspace beginning clean
- Renaming an Event
- Renaming a Scenario
- Duplicate Scenario-name rejection
- Deleting a non-final Scenario
- Blocking individual deletion of the final Scenario
- Deleting an Event and all its Scenarios after confirmation
- Restart persistence for renames and deletions
- Missing saved records producing friendly behavior

Focused comparison workflow

Add browser tests covering:

- Insufficient selection
- Comparing two Scenarios from one Event
- Comparing Scenarios from different Events
- Selecting three and four Scenarios
- Preventing selection beyond four
- Initial baseline assignment
- Changing the baseline
- Financial-result differences
- Percentage-point differences
- Assumption differences
- Target and warning differences
- Tie labels where fixture data creates ties
- Show differences only
- Opening every compared Scenario in the existing workspace
- Comparison creating no visible duplicate records

Focused backup and restore workflow

Add browser tests covering:

- Downloading a valid .ftbackup file
- The downloaded filename and extension
- Restoring that backup after changing current data
- Complete replacement rather than merging
- Setup destination after restoring data with Defaults
- Welcome destination after restoring a valid backup without Defaults, if such
  a supported backup state exists
- Invalid archive rejection
- Checksum-mismatch rejection
- Newer unsupported backup rejection
- Canceling restore
- Restore confirmation wording
- Current data remaining after restore validation failure
- Pre-restore recovery snapshot behavior where customer-visible or safely
  verifiable through the isolated test data root
- Restored data persisting after application restart

It is acceptable for narrowly technical backup-archive preparation in invalid
backup tests to use dedicated test helpers, but the actual upload, confirmation,
error, and successful restore must occur through the customer interface.

Application restart

Provide a reusable application restart fixture or helper that:

- Stops the current test Flask server cleanly
- Waits for its thread to terminate
- Preserves the test’s isolated application-data directory
- Starts a new application instance against that same directory
- Uses a new available loopback port if needed
- Opens a fresh browser context
- Does not retain stale cookies or temporary in-memory application state unless
  the specific test deliberately covers session behavior

Verify restart persistence for:

- Setup completion
- Defaults
- Events
- Scenarios
- Scenario names
- Ordered labor rows
- Ordered miscellaneous-cost rows
- Renames
- Deletions
- Restored backup data
- Migration version

Desktop-shell closure journey

Add a separate automated shell-lifecycle test using the real desktop shell
orchestrator with an injected headless test window adapter.

Do not require a graphical display.

The journey must:

1. Use an isolated application-data root.
2. Acquire the real single-instance lock.
3. Initialize and migrate a real temporary database.
4. Start the real loopback Werkzeug server.
5. Complete the readiness check.
6. Record that the headless window adapter received the application URL.
7. Trigger the same application-close callback used by pywebview.
8. Verify the server stops accepting connections.
9. Verify the server thread terminates.
10. Verify shell temporary files are cleaned where expected.
11. Verify the instance lock is released.
12. Start a second shell lifecycle successfully against the same data root.
13. Close it successfully again.

Also cover:

- Closing during the loading phase
- Repeated close signals
- Startup failure after acquiring the lock
- Startup failure after starting the server
- Cleanup after failure
- The database remaining valid after controlled shutdown

Do not claim this proves native window behavior on Windows or macOS.

Actual packaged-window closure remains a platform acceptance test.

Offline browser journey

Run at least one representative complete browser workflow while rejecting all
non-loopback network requests.

Allow:

- The application’s own 127.0.0.1 origin

Fail the test if the application attempts to require:

- Remote scripts
- Remote styles
- Remote fonts
- Remote images
- Remote APIs
- Telemetry
- Authentication

Do not block the browser’s internal implementation traffic in a way unrelated
to the application.

Cross-browser smoke suite

Mark a small critical smoke suite for Chromium, Firefox, and WebKit.

Include at least:

- Fresh onboarding and Defaults save
- Creating one Event
- Opening the Event Analysis workspace
- One live recalculation
- Saving a new Scenario
- Opening Saved Events
- Comparing two Scenarios

Run the complete browser suite in Chromium.

Do not require the entire full suite to run in all three engines for every
ordinary development run.

Diagnostics

Configure useful failure artifacts.

On failed browser tests, retain:

- A screenshot
- A Playwright trace
- Relevant browser console errors
- Relevant failed application requests
- Server log location for the isolated test run

Do not include customer or real developer data.

Do not retain screenshots or traces for every successful test unless
explicitly requested.

Store generated test artifacts under an ignored test-results directory.

Assertions

Assert customer-visible behavior and persisted behavior through the UI.

Prefer assertions such as:

- Current heading
- Status text
- Field values
- Visible financial result
- Visible Scenario name
- Saved Events grouping
- Comparison difference
- Success or validation message
- Correct route or workflow destination

Do not over-specify:

- Exact CSS classes
- Exact pixel positions
- Entire page HTML
- Incidental whitespace
- Current wall-clock timestamps
- Generated internal IDs

Use numeric assertions carefully.

When the displayed value is rounded for customers:

- Assert the documented displayed value.
- Rely on existing unit tests for exact internal Decimal precision.

Test-suite commands

Add clear pytest markers, such as:

- e2e
- e2e_smoke
- desktop_lifecycle

Provide documented commands for:

- Chromium full E2E suite
- Cross-browser smoke suite
- Desktop lifecycle suite
- Complete existing test suite
- Headed debugging
- Retaining and opening failure traces

Ensure ordinary unit tests do not automatically download browsers or open
windows.

Continuous-integration readiness

Make the tests suitable for later CI use.

- Keep browser installation explicit.
- Support headless execution.
- Use deterministic isolated directories.
- Avoid developer-machine paths.
- Avoid dependence on existing customer data.
- Avoid requiring internet access after required Playwright browsers have been
  installed.
- Return nonzero status on failures.
- Keep failure artifacts in predictable locations.

Do not add a new hosted CI service or workflow unless the repository already
has an established CI configuration that should naturally be extended.

Regression safety

Do not change production behavior merely to make tests pass.

Small testability improvements are allowed when they:

- Use dependency injection
- Expose clean lifecycle boundaries
- Permit isolated data roots
- Permit deterministic clocks where genuinely needed
- Preserve identical production behavior

Do not add publicly accessible test-only routes.

Do not weaken:

- Validation
- Route protection
- Transactions
- Restore verification
- Dirty-workspace protection
- Single-instance protection
- Navigation safeguards
- Calculation rules

Scope

Do not implement:

- Windows installer acceptance automation
- macOS DMG acceptance automation
- Native UI automation against packaged applications
- Code signing tests requiring real certificates
- Notarization tests requiring Apple credentials
- Automatic release publication
- New customer functionality
- New calculations
- New workflow behavior
- Visual screenshot-regression testing
- Performance load testing

Those platform-specific installation and packaged-application checks belong to
the next V1 acceptance-testing milestone.

Documentation

Add concise developer documentation covering:

- Required test dependencies
- Installing Playwright browser binaries
- E2E test architecture
- Test-data isolation
- Running Chromium tests
- Running cross-browser smoke tests
- Running headed tests
- Inspecting traces and screenshots
- Desktop-shell lifecycle testing
- Restart testing
- Offline testing
- Why real customer data must never be used
- What remains for Windows and macOS acceptance testing

Afterward:

1. Install only the required Playwright browser binaries when the environment
   permits it.
2. Run the focused Chromium E2E suite.
3. Run the cross-browser smoke suite when the browsers are available.
4. Run the desktop lifecycle suite.
5. Run the complete automated test suite.
6. Run git diff --check.
7. Summarize the automated customer journeys added.
8. Report every command and its exact result.
9. List all retained failure artifacts if anything failed.
10. Clearly report any unavailable browser binary, graphical capability, or
    environment limitation.

Do not make unrelated changes.
Do not commit.

Keep the response concise.

---------------------------------------------------------------------------------

Implement the milestone:

Complete platform acceptance testing and fix V1 defects.

The Windows and macOS packages, desktop shell, complete automated end-to-end
suite, calculations, persistence, backup and restore, accessibility, and all V1
customer workflows already exist.

Perform release-candidate acceptance testing against the actual packaged
Windows and macOS applications. Resolve every confirmed release-blocking defect
that can be reproduced, then rebuild and retest the affected artifacts.

Do not treat source-mode Flask execution as platform acceptance testing.

Acceptance-test records

Create a structured V1 acceptance-test record in the repository.

For every performed check, record:

- Test identifier
- Platform
- Architecture
- Operating-system version
- Application version
- Artifact filename
- Artifact SHA-256 checksum
- Clean, upgrade, reinstall, or recovery environment
- Preconditions
- Steps performed
- Expected result
- Actual result
- Pass, fail, blocked, or not run
- Evidence or concise notes
- Related defect identifier when applicable
- Retest result after a fix

Clearly distinguish:

- Passed
- Failed
- Blocked by an unavailable environment or credential
- Not run

Never mark an unavailable check as passed.

Release-blocking definition

Treat a confirmed defect as release-blocking when it causes or creates a
material risk of:

- Failure to install
- Failure to launch
- Failure to close cleanly
- Local server or process remaining active after application closure
- Data loss
- Data corruption
- Partial database writes
- Unsafe schema migration
- Customer data being written into installed application resources
- Customer data being deleted during upgrade, reinstall, replacement, or
  uninstall
- Backup creation failure in an otherwise supported state
- Valid backup restoration failure
- Invalid backup being accepted
- Recovery safeguards failing
- Incorrect financial calculations
- Incorrect profit, margin, break-even, demand, or warning results
- Saved assumptions loading incorrectly
- A core V1 workflow being unusable
- Scenario version actions overwriting the wrong Scenario
- Comparison displaying results from the wrong assumptions
- Setup or route safeguards allowing an unusable application state
- Single-instance protection allowing conflicting writes
- Required offline workflows depending on external network access
- A serious keyboard or screen-reader barrier that prevents completion of a
  core workflow
- Windows prerequisite handling preventing supported installation
- macOS signing, notarization, Gatekeeper, architecture, or bundle defects that
  prevent the intended supported distribution

Do not classify as release-blocking solely because of:

- Minor spacing inconsistencies
- Small wording preferences
- Nonessential animation or transition behavior
- Cosmetic differences between platforms
- Development-only unsigned-build warnings that are already documented and do
  not affect the intended signed release process

Document nonblocking defects separately without expanding this milestone into
new product work.

Release candidates

Test actual newly built release-candidate artifacts.

Windows:

- PyInstaller one-folder application
- Inno Setup installer

macOS:

- py2app application bundle
- Drag-and-drop DMG

Use the centralized application-version source.

Generate and record SHA-256 checksums for every tested artifact.

Do not silently test an older artifact after source changes.

After any packaging-affecting fix:

- Rebuild the affected artifact.
- Generate a new checksum.
- Repeat all acceptance checks materially affected by the change.

Windows test matrix

Where clean environments are available, test:

- Windows 10 x64
- Windows 11 x64

For each tested Windows version, cover the following.

Clean installation:

- Installer launches without requiring administrator elevation.
- WebView2 detection works when the Runtime is already installed.
- Bundled offline WebView2 installation works when the Runtime is absent.
- A WebView2 prerequisite failure stops installation safely.
- Application files install under the configured per-user program location.
- Customer data is created under the configured LocalAppData location.
- No mutable customer data is written beside the executable.
- Start Menu shortcut works.
- Desktop shortcut behavior matches the installer choice.
- Add/Remove Programs metadata is correct.
- Application launches without a console window.
- Product title, version, and icon are correct.
- First-use onboarding works.

Core customer workflow:

- Save Business Defaults.
- Create a complete Event.
- Verify automatic Original estimate creation.
- Use live Event Analysis recalculation.
- Save as a new Scenario.
- Overwrite the active Scenario deliberately.
- Open saved work.
- Rename and delete permitted records.
- Compare Scenarios.
- Download a backup.
- Restore a valid backup.
- Reject an invalid backup.
- Verify restored data after restart.

Desktop behavior:

- Second launch is rejected safely.
- Closing the window stops the local server.
- Closing releases the instance lock.
- Application can start again afterward.
- Window resizing preserves usable layouts.
- Keyboard navigation remains usable.
- No ordinary workflow opens a browser window unexpectedly.

Existing data and upgrade:

- Install over an older supported package.
- Preserve Defaults, Events, Scenarios, backups, and recovery snapshots.
- Run pending database migrations safely.
- Produce only one installed-program entry.
- Preserve working shortcuts.
- Open the correct Dashboard or Welcome state afterward.

Same-version reinstall:

- Reinstall successfully.
- Preserve all customer data.
- Avoid duplicate shortcuts and installed-program entries.

Uninstall and reinstall:

- Remove installer-owned application files and shortcuts.
- Preserve LocalAppData customer records.
- Reinstall successfully.
- Reconnect to preserved customer data.

Offline operation:

- Install without internet access.
- Launch without internet access.
- Complete representative core workflows without internet access.
- Use only the local loopback server.

Failure and recovery:

- Damaged database enters the established recovery state.
- Damaged data is not silently deleted or replaced.
- Restore remains available where designed.
- Interrupted or failed startup preserves customer data.
- Installer failure does not remove established customer data.
- Running-application installation and uninstall handling is safe.

macOS test matrix

Where suitable environments are available, test:

- Apple Silicon arm64
- Intel x86_64
- macOS 13 or later

Verify the actual architecture slice used at runtime.

Do not count Rosetta execution as native Apple Silicon acceptance.

For each tested Mac environment, cover the following.

DMG and installation:

- DMG mounts successfully.
- Volume identity is correct.
- Application and Applications alias are present.
- Application copies successfully to /Applications or ~/Applications.
- Application launches without Terminal.
- Bundle name, version, identifier, and icon are correct.
- Customer data is created only under the configured Application Support
  directory.
- No mutable data is written into the .app bundle.
- First-use onboarding works.

Core customer workflow:

- Save Business Defaults.
- Create a complete Event.
- Verify automatic Original estimate creation.
- Use live Event Analysis recalculation.
- Save as a new Scenario.
- Overwrite the active Scenario.
- Open saved work.
- Rename and delete permitted records.
- Compare Scenarios.
- Download a backup.
- Restore a valid backup.
- Reject an invalid backup.
- Verify restored data after restart.

Desktop behavior:

- Second launch is rejected safely.
- Closing the window stops the local server.
- Closing releases the instance lock.
- Application launches again successfully.
- Window resizing preserves usable layouts.
- Keyboard navigation remains usable.
- No ordinary workflow opens a browser unexpectedly.

Application replacement:

- Replace an older .app with the newer release candidate.
- Preserve Defaults, Events, Scenarios, backups, and recovery snapshots.
- Run pending schema migrations safely.
- Continue using the same Application Support directory.
- Open the correct application state.

Uninstall and reinstall:

- Move the .app to Trash.
- Preserve Application Support customer data.
- Reinstall from the DMG.
- Reconnect to preserved data.

Offline operation:

- Install from the DMG without internet access.
- Launch without internet access.
- Complete representative workflows without internet access.

Failure and recovery:

- Damaged database enters the established recovery state.
- Damaged data remains preserved.
- Restore remains available where designed.
- Failed startup does not replace or reset customer data.

macOS release verification

When valid Developer ID and notarization credentials are available:

- Sign nested executable code correctly.
- Sign the outer application bundle last.
- Verify Hardened Runtime.
- Verify bundle identifier and version.
- Verify arm64 and x86_64 slices.
- Create and sign the DMG.
- Submit using notarytool.
- Verify successful notarization.
- Staple the ticket.
- Verify the stapled ticket.
- Verify Gatekeeper acceptance.

When credentials or required hardware are unavailable:

- Record the checks as blocked or not run.
- Do not claim the artifact is release-ready for macOS distribution.
- Do not claim signing or notarization success.
- Preserve development-build results separately.

Cross-platform data checks

On both platforms, verify:

- Fresh databases are created through the migration system.
- Existing compatible databases retain every identifier and relationship.
- Business Defaults persist.
- Events and Scenarios persist.
- Ordered employee labor rows persist.
- Ordered miscellaneous-cost rows persist.
- Calculated results are regenerated from saved assumptions.
- Backup files contain the expected data.
- Restore performs complete replacement rather than merging.
- Automatic recovery snapshots remain valid.
- Application upgrades do not relocate or duplicate customer data.
- Uninstallation or application replacement does not delete customer data.

Cross-platform calculation checks

Using the same manually verifiable fixtures on both platforms, verify displayed:

- Weather-adjusted attendance
- Total expected food buyers
- Even-split expected buyers
- Expected sales
- Food and packaging cost
- Payment-processing costs
- Labor
- Owner labor pay
- Total event cost
- Business profit
- Profit margin
- Break-even sales
- Break-even customers
- Profit-target evaluation
- Factual warnings

Compare displayed values with the established manual examples and automated
calculation tests.

Any platform-specific arithmetic difference is release-blocking.

Accessibility acceptance

On each available platform, perform practical acceptance checks for:

- Complete keyboard use of a core customer journey
- Visible focus
- Dialog focus entry and restoration
- Validation summary and inline errors
- Repeatable labor and miscellaneous-cost rows
- Scenario save and overwrite dialogs
- Saved Event rename and delete flows
- Scenario comparison
- Backup and restore
- 200% zoom or equivalent scaling
- Narrow supported window sizing
- Long Event and Scenario names
- No essential information conveyed only through color

Where available, perform a basic screen-reader workflow using the platform’s
standard assistive technology.

Record the exact assistive technology and version used.

A barrier that prevents completion of a core workflow is release-blocking.

Defect workflow

Create a concise acceptance-defect log.

For every confirmed defect, record:

- Defect identifier
- Environment
- Artifact checksum
- Reproduction steps
- Expected behavior
- Actual behavior
- Severity
- Release-blocking decision and rationale
- Root cause when identified
- Files changed
- Automated regression coverage added
- Focused test result
- Full-suite result
- Rebuilt artifact checksum
- Acceptance retest result

Before fixing:

- Reproduce the problem reliably.
- Confirm it occurs in the tested release candidate.
- Distinguish product defects from test-environment failures.

For every release-blocking defect:

1. Add or update an automated regression test where technically practical.
2. Fix the smallest underlying cause.
3. Do not work around data-integrity or calculation problems only in the UI.
4. Run focused tests.
5. Run the full automated suite.
6. Run git diff --check.
7. Rebuild the affected packaged artifact.
8. Repeat the failed acceptance test.
9. Repeat adjacent critical workflows that could have regressed.
10. Update the acceptance record and defect log.

If a blocker cannot be fixed safely within this milestone:

- Leave it open and clearly documented.
- Do not mark the platform release-ready.
- Do not conceal it with wording changes or skipped tests.

Change control

Do not add:

- New business features
- New calculations
- New Scenario behavior
- New Dashboard features
- New comparison behavior
- New backup formats
- Automatic updates
- Cloud services
- Telemetry
- License systems
- Unplanned redesign

Small usability corrections are allowed only when required to resolve a
confirmed acceptance defect.

Do not broadly refactor stable systems without a defect-driven reason.

Automated regression testing

After acceptance fixes:

- Run all focused tests related to changed behavior.
- Run the complete unit, integration, route, migration, backup, shell,
  packaging, accessibility, and end-to-end test suite.
- Run the Chromium full E2E suite.
- Run the Firefox and WebKit smoke suites where browser binaries are available.
- Run desktop lifecycle tests.
- Run offline tests.
- Run packaging audits.
- Run git diff --check.

Do not weaken existing tests to make acceptance fixes pass.

Release-readiness summary

Create a final V1 acceptance summary containing:

- Application version
- Source revision
- Tested artifact names and checksums
- Platforms and architectures tested
- Clean-install results
- Upgrade and replacement results
- Reinstall results
- Uninstall and data-preservation results
- Offline results
- Core-workflow results
- Backup, restore, and recovery results
- Accessibility results
- Signing, notarization, and Gatekeeper results
- Automated-suite results
- Fixed release blockers
- Remaining release blockers
- Remaining nonblocking defects
- Blocked or unperformed checks
- Overall readiness separately for:
  - Windows
  - macOS

Use only these readiness outcomes:

- Ready for release
- Not ready for release
- Acceptance incomplete

Do not describe a platform as ready when required release-blocking checks are
failed, blocked, or unperformed.

Documentation

Update packaging and release documentation with:

- Final performed acceptance procedure
- Test environments used
- Required clean-environment checks
- Defect-classification rules
- Artifact-evidence requirements
- Rebuild and retest procedure
- Known environment limitations
- Remaining manual checks before publication

Scope

Do not:

- Publish V1
- Tag version 1.0.0
- Upload release artifacts
- Announce release readiness without evidence
- Add unsupported platforms
- Generate fake acceptance evidence
- Claim unavailable hardware or credentials were used
- Commit signing secrets or credentials

Do not make unrelated changes.
Do not commit.

Afterward:

1. Report every environment actually used.
2. Report every tested artifact path and checksum.
3. Report all acceptance checks as passed, failed, blocked, or not run.
4. List every confirmed defect.
5. List every release-blocking defect fixed.
6. List every open release blocker.
7. Report all focused and full-suite test commands and exact results.
8. Report all rebuilt artifact paths and checksums.
9. State Windows readiness.
10. State macOS readiness.
11. Clearly identify hardware, credentials, packaging tools, or clean
    environments that were unavailable.

Keep the response concise but do not omit limitations.

---------------------------------------------------------------------------------

