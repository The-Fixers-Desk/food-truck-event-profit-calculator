Next Codex Prompt

---------------------------------------------------------------------------------

Implement the milestone:

Complete saved event and scenario management.

Automatic initial saving, scenario version actions, the interactive Event
Analysis workspace, calculation engine, persistence schema, and transactional
repositories already exist.

Build the customer-facing Saved Events management workflow without duplicating
the workspace or calculation logic.

Saved Events page

Add a Saved Events screen accessible through the existing application
navigation.

Group saved Scenarios beneath their parent Event.

For each Event, display:

- Event name
- Event date
- Start time
- Location
- Number of saved Scenarios
- Last modified time

For each Scenario, display:

- Scenario name
- Last modified time
- An “Open analysis” action
- Rename action
- Delete action

Order Events by most recently modified first.

Order Scenarios within each Event by most recently modified first.

Use deterministic secondary ordering when timestamps are equal.

Include a useful empty state when no Events have been saved.

Do not build the Dashboard in this milestone.

Opening a saved Scenario

When “Open analysis” is selected:

- Load the Event identity.
- Load the Scenario’s complete saved assumptions.
- Preserve the order of employee labor and miscellaneous-cost entries.
- Validate the persisted assumptions through the existing domain behavior.
- Recalculate all results, break-even values, target evaluations, and warnings
  through the existing Python calculation engine.
- Open the existing interactive Event Analysis workspace.
- Make the selected Scenario the active Scenario.
- Use the saved assumptions as the Reset changes baseline.
- Begin with a clean, non-dirty workspace state.

Do not create a second workspace implementation.

Do not load or persist calculated results.

If an Event or Scenario does not exist, show the application’s normal
customer-facing not-found behavior rather than crashing.

Renaming Events

Allow the Event name to be renamed.

Rules:

- Require a nonblank name after trimming surrounding whitespace.
- Event names do not need to be unique.
- Update only the Event name and modified timestamp.
- Do not change its date, start time, location, Scenarios, or assumptions.
- Preserve the entered value and show a clear validation error after failure.

Do not add general Event identity editing in this milestone.

Renaming Scenarios

Allow an individual Scenario to be renamed.

Rules:

- Require a nonblank name after trimming surrounding whitespace.
- Scenario names must remain unique within the same Event.
- Compare uniqueness without regard to capitalization or surrounding spaces.
- The same name may be used under different Events.
- Do not silently add numbers or alter duplicate names.
- Update only the Scenario name and modified timestamp.
- Do not change its assumptions or sibling Scenarios.
- If the renamed Scenario is currently active in the workspace, ensure its
  displayed active name is correct when next loaded or refreshed.

Delete Scenario

Add a confirmed Scenario deletion action.

- Clearly identify the Scenario being deleted.
- Delete only the selected Scenario.
- Do not modify sibling Scenarios or the parent Event.
- Do not allow the final remaining Scenario belonging to an Event to be
  deleted individually.
- When deletion is blocked, explain that the complete Event can be deleted
  instead.
- After successful deletion, remain on the Saved Events screen and show a
  success state.

Delete Event

Add a separate confirmed Event deletion action.

- Clearly identify the Event.
- State how many saved Scenarios will also be deleted.
- Require explicit confirmation.
- Delete the Event and all of its Scenarios and ordered child records through
  the established cascading relationships.
- Perform the deletion transactionally.
- After success, remain on the Saved Events screen and show a success state.

Do not allow a failed deletion to leave partially deleted data.

Unsaved-change protection

Reuse the workspace’s existing dirty-state behavior.

When the active workspace contains unsaved changes and the customer attempts
to:

- Navigate to Saved Events
- Open a different saved Scenario
- Leave through another application navigation action that would discard the
  workspace state

require confirmation before discarding the unsaved changes.

If the customer cancels:

- Remain in the current workspace
- Preserve all assumptions, rows, validation state, and current results

If the customer confirms:

- Continue to the requested destination
- Do not alter the active saved Scenario

Do not show a discard warning when the workspace is clean.

Persistence after restart

Verify that after closing and recreating the application:

- Saved Events remain listed
- Scenario names and Event names remain updated
- Deleted records remain deleted
- Ordered child rows remain intact
- Any remaining Scenario can be loaded into the workspace
- Recalculated results match its saved assumptions

Data integrity

- Reuse the existing repositories and transaction patterns.
- Continue using scaled integer persistence conventions.
- Do not delete, reset, or manually replace data/app.db.
- Preserve Business Defaults and unrelated customer data.
- Do not add stored calculated-result columns.
- Do not create duplicate Event or Scenario records merely by opening them.

Scope

Do not implement:

- Scenario comparison
- Financial comparison summaries
- Event date, time, or location editing
- Moving a Scenario between Events
- Scenario reordering
- Dashboard behavior
- Search, filtering, or pagination
- Backup and restore
- Final UI and UX polish

Do not add dependencies.
Do not make unrelated changes.
Do not commit.

Testing

Add or update tests covering:

- Saved Events empty state
- Events grouped with their Scenarios
- Event and Scenario ordering
- Complete Event identity display
- Opening every saved Scenario in the existing workspace
- Saved assumptions and ordered rows loading correctly
- Results being recalculated rather than loaded from storage
- Loaded Scenario becoming the active reset baseline
- Loaded workspace beginning clean
- Missing Event and Scenario handling
- Valid and invalid Event-name changes
- Event renaming leaving identity and Scenarios unchanged
- Valid Scenario renaming
- Blank Scenario-name validation
- Duplicate, trimmed, and case-insensitive Scenario-name validation
- Same Scenario name under different Events
- Deleting a non-final Scenario
- Preventing individual deletion of the final Scenario
- Confirmed Event deletion cascading to all Scenarios and children
- Transaction rollback after deletion failure
- Unsaved-change confirmation when leaving a dirty workspace
- Cancellation preserving the workspace
- Clean navigation requiring no confirmation
- Persistence after application restart
- Business Defaults and unrelated data remaining unchanged
- Opening a Scenario creating no duplicate records

Afterward:

1. Run focused saved-work, repository, route, and workspace tests.
2. Run the full test suite.
3. Run git diff --check.
4. Summarize the customer-visible management workflow.
5. Explain how to manually test opening, renaming, deleting, dirty-state
   protection, and restart persistence.
6. Report the test results.

Keep the response concise.

---------------------------------------------------------------------------------

Implement the milestone:

Build the complete event comparison workflow.

Saved Event and Scenario management, the interactive Event Analysis workspace,
scenario persistence, and the complete calculation engine already exist.

Build a read-only comparison workflow for saved Scenarios. Reuse existing
repositories, models, calculation logic, warnings, and workspace behavior.

Scenario selection

Allow customers to select saved Scenarios for comparison from the Saved Events
screen.

- Require at least two Scenarios.
- Allow no more than four Scenarios.
- Allow Scenarios from the same Event.
- Allow Scenarios from different Events.
- Do not allow the same Scenario to be selected more than once.
- Identify every selection using both:
  - Event name
  - Scenario name
- Add a “Compare selected” action.
- Disable or reject comparison when fewer than two Scenarios are selected.
- Show a clear validation message when the customer attempts to select more
  than four.

Preserve the existing Saved Events grouping and management actions.

Comparison calculations

For every selected Scenario:

- Load the Event identity and complete saved assumptions.
- Preserve ordered employee labor and miscellaneous-cost entries.
- Validate the persisted assumptions through existing domain behavior.
- Recalculate all financial results using the existing Python calculation
  engine.
- Include current break-even values, profit-target evaluation, and factual
  warnings.

Do not:

- Read calculated results from the database
- Persist comparison results
- Duplicate calculation formulas in JavaScript or templates
- Create Event or Scenario records while comparing

Missing or invalid saved data should produce a customer-facing error without
crashing the complete comparison.

Baseline behavior

Use the first selected Scenario as the initial comparison baseline.

Clearly label it:

“Baseline”

Allow the customer to make any selected Scenario the baseline without leaving
or rebuilding the comparison.

Changing the baseline must update displayed differences but must not alter any
saved data.

Difference rules

Compare every non-baseline Scenario with the active baseline.

For money values and customer or order counts:

Difference =
compared Scenario value - baseline value

For percentages, rates, and profit margin:

- Display the difference in percentage points.
- Do not describe a percentage-point difference as a relative percentage
  increase or decrease.

For categorical assumptions:

- Show the baseline value and compared value.
- Highlight the row when values differ.

For unavailable results:

- Display “Not available.”
- Do not calculate or display a misleading numeric difference.

Use exact calculation values for comparisons and apply customer-facing display
rounding only when rendering.

Comparison summary

For each selected Scenario, display:

- Event name
- Scenario name
- Event date and start time
- Location
- Expected buyers
- Expected orders
- Expected sales
- Variable costs
- Fixed costs
- Employee labor
- Owner labor pay
- Total event cost
- Business profit
- Profit margin
- Break-even sales
- Exact break-even customers
- Minimum whole break-even customers
- Profit-target status
- Current factual warnings

Clearly distinguish owner labor pay from business profit.

Meaningful factual highlights

Across selected Scenarios, factually identify:

- Highest business profit
- Lowest total event cost
- Lowest minimum whole break-even customer requirement
- Scenarios meeting their selected profit target
- Scenarios not meeting their selected profit target
- Warnings that appear in only some selected Scenarios

Handle ties accurately.

Do not:

- Declare one Scenario the winner
- Recommend accepting or rejecting an Event
- Create arbitrary scoring
- Add arbitrary definitions of a good or bad result

Assumption comparison

Compare the saved assumptions that produced each result, including:

Demand:
- Estimated attendance
- Weather outlook
- Custom weather reduction
- Event protection
- Percentage expected to buy food
- Other competing food vendors
- Total vendor count
- Even-split expected buyers

Revenue:
- Average order amount
- Calculated or custom expected-sales method
- Custom expected-sales amount when active

Food and payment costs:
- Food and packaging cost method and value
- Percentage of sales paid by card
- Card-processing percentage
- Fixed card-processing fee per transaction

Labor:
- Employee labor entries
- Total employee labor cost
- Owner labor pay

Event costs:
- Travel
- Parking
- Permit
- Generator or utility cost
- Vendor or booking fee
- Organizer commission
- Named miscellaneous costs

Profit target:
- Target type
- Target value

For repeatable labor and miscellaneous-cost entries:

- Provide a readable summary for each Scenario.
- Preserve entry order.
- Highlight the category when the ordered entries differ.
- Do not attempt to infer that differently named rows represent the same cost.

Differences-only control

Add a control labeled:

“Show differences only”

When active:

- Hide assumption rows whose values are identical across every selected
  Scenario.
- Keep the principal financial-result summary visible.
- Keep Event and Scenario identity visible.
- Do not hide unavailable or warning information merely because it appears in
  multiple Scenarios.

Opening a Scenario

Provide an “Open analysis” action for every compared Scenario.

When selected:

- Open the existing interactive Event Analysis workspace.
- Load that saved Scenario as the active Scenario.
- Use its saved assumptions as the Reset changes baseline.
- Recalculate through the existing engine.
- Begin with a clean workspace and no unsaved changes.
- Do not create duplicate records.

Reuse the existing unsaved-change navigation protection when comparison is
entered from a dirty workspace.

Layout and accessibility

Create a functional, readable comparison layout.

For wider windows:

- Display selected Scenarios in aligned comparison columns.
- Keep row labels clear.
- Avoid losing the relationship between a value and its Scenario.

For narrower windows:

- Use readable stacked Scenario sections or another responsive structure.
- Do not force the entire application page into horizontal scrolling.
- Preserve logical keyboard and screen-reader order.

Use proper table or semantic comparison structure where appropriate.

Ensure:

- Scenario-selection controls have accessible labels
- Baseline changes are announced
- Comparison validation is accessible
- Highlights are conveyed through text, not color alone

Do not perform final visual polish in this milestone.

Empty and error states

Support:

- No saved Events
- Only one saved Scenario
- Fewer than two selected Scenarios
- More than four attempted selections
- A selected Scenario being deleted or missing before comparison loads
- Saved assumptions that cannot be validated or calculated

Provide clear next actions where possible.

Persistence and data integrity

- Comparison must not modify Events, Scenarios, Business Defaults, or ordered
  child rows.
- Do not save comparison selection sets.
- Do not add stored calculation-result columns.
- Do not delete, reset, or replace data/app.db.
- Preserve unrelated customer data.

Scope

Do not implement:

- Editing assumptions directly in comparison
- Saving a Scenario from the comparison page
- Automated recommendations
- Scenario scoring
- Charts requiring new dependencies
- Search, filtering, or pagination
- Dashboard behavior
- Final UI and UX polish

Do not add dependencies.
Do not make unrelated changes.
Do not commit.

Testing

Add or update tests covering:

- Selecting two, three, and four Scenarios
- Comparing Scenarios from one Event
- Comparing Scenarios from different Events
- Preventing duplicate selections
- Fewer than two selection validation
- More than four selection validation
- First selected Scenario becoming the baseline
- Changing the active baseline
- Recalculating each Scenario from saved assumptions
- Comparison creating or modifying no database records
- Money and count differences
- Percentage-point differences
- Categorical assumption differences
- Unavailable values
- Highest-profit highlighting
- Lowest-cost highlighting
- Lowest-break-even highlighting
- Accurate tie handling
- Profit-target comparison
- Warning differences
- Complete assumption comparison
- Ordered labor and miscellaneous-cost differences
- Show differences only behavior
- Opening each compared Scenario in the existing workspace
- Opened workspace beginning clean
- Missing and invalid Scenario handling
- Empty and insufficient-data states
- Business Defaults and saved Scenarios remaining unchanged
- Accessible and responsive comparison structure where practical

Afterward:

1. Run focused comparison, repository, route, and workspace tests.
2. Run the full test suite.
3. Run git diff --check.
4. Summarize the customer-visible comparison workflow.
5. Explain how to manually test:
   - Same-Event comparison
   - Different-Event comparison
   - Baseline changes
   - Differences-only behavior
   - Opening a Scenario
6. Report the test results.

Keep the response concise.

---------------------------------------------------------------------------------

Implement the milestone:

Build the Dashboard and first-use application flow.

Business Defaults, Event Inputs, automatic Event and Scenario saving, the
interactive analysis workspace, Saved Events management, and Scenario
comparison already exist.

Build the first-launch onboarding flow and the normal Dashboard entry point.
Reuse the existing screens, routes, navigation, repositories, and dirty-state
protection.

Setup-completion rule

Determine whether initial setup is complete from the existence of a valid saved
Business Defaults record.

- Do not add a separate onboarding-complete database flag.
- Do not use a session-only value as the source of truth.
- Existing installations with valid saved Business Defaults must be treated as
  already set up.
- Missing or invalid Business Defaults mean setup is incomplete.

Welcome screen

Add a first-launch Welcome screen that introduces this four-step workflow:

1. Set business defaults
2. Describe an event
3. Review the analysis
4. Compare options

Include concise customer-facing explanations for each step.

Add one clear primary action labeled:

“Get started”

When selected:

- Open the existing Business Defaults screen.
- Treat the visit as part of initial setup.
- Do not create placeholder Defaults, Events, or Scenarios.

Do not perform final visual polish yet. Use the project’s existing readable
styling patterns.

First-use flow

When no valid Business Defaults exist:

- Application launch opens Welcome.
- Get started opens Business Defaults.
- A successful first Business Defaults save opens Dashboard.
- A failed Defaults save remains on the Defaults screen with existing errors
  and preserved values.
- Closing and reopening before setup is complete begins at Welcome again.

When valid Business Defaults already exist:

- Application launch opens Dashboard.
- Welcome redirects safely to Dashboard.
- Do not repeat onboarding.

Dashboard

Build the Dashboard as the normal application home screen after setup.

Provide these primary actions:

1. Analyze new event
   - Open a clean Event Inputs workflow.
   - Prefill applicable values from current Business Defaults.
   - Do not reuse assumptions from the previous Event or workspace.

2. View saved events
   - Open the existing Saved Events screen.
   - Keep this action available when no Events exist.

3. Compare scenarios
   - Open the existing Scenario comparison selection workflow when at least two
     saved Scenarios exist.
   - When fewer than two saved Scenarios exist, present a clear unavailable or
     empty state explaining that at least two saved Scenarios are required.
   - Do not silently send the user to an unusable comparison page.

4. Update business defaults
   - Open the existing Business Defaults screen in normal editing mode.
   - Saving updated Defaults must not alter existing Events or Scenarios.
   - After a successful normal update, preserve the existing confirmation
     behavior and provide a clear way back to Dashboard.
   - Do not treat a normal Defaults update as onboarding.

Dashboard information and states

Include concise useful context such as:

- Whether Business Defaults are available
- Number of saved Events
- Number of saved Scenarios
- A useful first-event prompt when no Events exist
- A comparison-readiness message when fewer than two Scenarios exist

Do not add financial summaries, charts, recommendations, recent-activity
systems, or stored Dashboard analytics in this milestone.

Navigation

Update the shared application navigation so that after setup it provides clear
access to:

- Dashboard
- Analyze new event
- Saved events
- Compare scenarios
- Business defaults

Before setup is complete:

- Welcome and Business Defaults may be accessed.
- Dashboard, Event Inputs, Event Analysis workspaces, Saved Events, and
  Comparison must not bypass setup.
- Direct requests to setup-protected routes must redirect safely to Welcome or
  Business Defaults rather than crashing or rendering incomplete workflows.
- Preserve an appropriate destination message when useful.

After setup is complete:

- Welcome redirects to Dashboard.
- Dashboard acts as the normal home route.

Unsaved workspace protection

Preserve and reuse the existing dirty-workspace navigation protection.

If the customer attempts to use Dashboard or navigation links while the active
workspace contains unsaved changes:

- Require confirmation before discarding those changes.
- Canceling must preserve the workspace exactly.
- Confirming must navigate without modifying the active saved Scenario.
- Do not show the warning when the workspace is clean.

Application and session behavior

Use persisted Business Defaults as the setup source of truth across application
restarts.

Use session state only for temporary navigation or customer feedback where
appropriate.

Do not let stale session data:

- Skip required setup
- Repeat onboarding after setup
- Reopen a deleted Event or Scenario
- Override the current database state

Empty and error states

Support:

- No Business Defaults
- No saved Events
- One saved Scenario
- Fewer than two Scenarios available for comparison
- Saved records disappearing before a shortcut opens
- Repository or database failures while loading Dashboard counts

Provide clear next actions and use existing application-wide error handling.

Data integrity

- Dashboard and onboarding views must not create Events or Scenarios.
- Do not modify existing Event or Scenario assumptions.
- Do not reset or replace data/app.db.
- Preserve Business Defaults and all unrelated customer data.
- Reuse current repositories and counting methods where practical.

Scope

Do not implement:

- Final visual design
- Charts or financial Dashboard summaries
- Recent-event history
- Search, filtering, or pagination
- Backup and restore
- Desktop packaging
- New calculation behavior
- Changes to Scenario comparison rules
- Changes to saved-work management behavior

Do not add dependencies.
Do not make unrelated changes.
Do not commit.

Testing

Add or update tests covering:

- Fresh application launch opening Welcome
- Four-step workflow content
- Get started opening Business Defaults
- First successful Defaults save opening Dashboard
- Failed first save preserving form values and errors
- Reopening before setup remains complete
- Existing saved Defaults skipping Welcome
- Normal launches opening Dashboard
- Welcome redirecting after setup
- Dashboard action destinations
- New Event Inputs beginning clean and using current Defaults
- Saved Events empty-state access
- Comparison unavailable with zero or one Scenario
- Comparison available with at least two Scenarios
- Accurate Event and Scenario counts
- Updating Defaults not modifying saved Events or Scenarios
- Setup-protected direct routes redirecting safely
- Dirty-workspace navigation confirmation
- Clean-workspace navigation without confirmation
- Database state overriding stale session state
- Persistence of setup completion after application restart
- Dashboard repository-error handling
- No onboarding or Dashboard action creating unintended records

Afterward:

1. Run focused onboarding, Dashboard, route, and navigation tests.
2. Run the full test suite.
3. Run git diff --check.
4. Summarize the first-use and normal-launch workflows in plain language.
5. Explain how to manually test fresh setup, later launches, Dashboard empty
   states, shortcuts, route safeguards, and dirty-workspace protection.
6. Report the test results.

Keep the response concise.

---------------------------------------------------------------------------------

Implement the milestone:

Add database versioning and safe migrations.

The application currently has a working SQLite schema containing Business
Defaults, Events, Event Scenarios, employee labor rows, miscellaneous-cost
rows, and other established application data.

Add a formal migration system that supports safe future schema upgrades while
preserving all existing customer data.

Version 1 baseline

Treat the complete schema that exists at the beginning of this milestone as
the official Version 1 schema.

Inspect the current database initialization, repositories, tables, columns,
indexes, foreign keys, constraints, and scaled-integer persistence conventions
before changing the initialization process.

Do not change the meaning or structure of existing application data merely to
introduce versioning.

Migration ledger

Add a migration ledger table named:

schema_migrations

Store at least:

- version
- name
- applied_at

Rules:

- Version must be a unique sequential integer.
- Migration records are immutable after successful application.
- Record a migration only after its complete schema operation and verification
  succeed.
- The highest successfully recorded version represents the current database
  schema version.
- Define the application’s latest supported schema version explicitly.

Do not use session state or application memory as the source of schema version
truth.

Fresh database behavior

A completely new empty database must:

- Create the migration ledger.
- Apply the Version 1 migration.
- Create the complete current application schema.
- Record Version 1 only after schema creation succeeds.
- Be immediately usable by all existing application workflows.

Fresh setup and existing-database upgrades must produce the same final schema.

Existing unversioned database adoption

Existing customer databases currently have application tables but no migration
ledger.

Support adopting a compatible unversioned database as Version 1 without
rebuilding or rewriting it.

Before adoption:

- Inspect all expected Version 1 tables.
- Verify required columns and compatible types.
- Verify important indexes, foreign keys, and relationships.
- Verify the expected Defaults, Event, Scenario, labor, and miscellaneous-cost
  structures.
- Run an appropriate SQLite integrity or quick check.
- Run a foreign-key check where applicable.

When the unversioned database is compatible:

- Create the migration ledger safely.
- Record Version 1.
- Preserve every existing row and identifier.
- Do not rename, recreate, copy, or reorder existing business data merely to
  stamp the version.

When the database is incomplete, conflicting, or not recognizably compatible:

- Do not stamp it as Version 1.
- Do not attempt destructive repairs.
- Stop startup safely.
- Log the technical reason.
- Show the customer the application’s normal friendly database-startup error.

Do not silently reinterpret an unknown schema.

Already-versioned database behavior

When the database is already at the latest supported version:

- Perform no migration writes.
- Do not update migration timestamps.
- Do not rerun migration functions.
- Continue normal application startup.

When the database is behind the latest supported version:

- Apply every missing migration in numerical order.
- Do not skip versions.
- Stop immediately if any migration fails.

When the database version is newer than the application supports:

- Refuse to open it for normal operation.
- Do not downgrade it.
- Do not modify its schema or version records.
- Provide a clear error indicating that a newer application version is
  required.

Migration execution

Create a clear migration registry or equivalent ordered structure containing:

- Version number
- Migration name
- Migration function

Migration execution must occur:

- After the database path and connection behavior are established
- Before repositories, routes, or normal application workflows use the schema

Each migration must:

- Use the project’s existing transaction behavior.
- Be atomic.
- Validate its expected starting state.
- Apply only its intended changes.
- Validate its resulting state.
- Record its version last.

If any schema operation, data-copy operation, verification step, or ledger
write fails:

- Roll back the complete migration.
- Leave the database at its previous successfully recorded version.
- Do not continue to later migrations.
- Do not allow normal application startup.

Do not broadly catch and ignore SQLite schema errors.

Temporary-table safety

For migrations that eventually need table reconstruction:

- Use a unique migration-specific temporary table name.
- Do not use generic names such as prior_defaults.
- Verify that the intended temporary name does not unexpectedly exist.
- Treat an unexpected leftover table as a migration conflict rather than
  silently reusing or deleting it.
- Copy data explicitly.
- Verify copied row counts and required relationships before replacing the
  original table.
- Drop old or temporary structures only after successful verification.

Document this required pattern even if Version 1 itself does not need table
reconstruction.

Schema initialization

Replace or adapt the existing parallel schema-initialization behavior so schema
creation and upgrading are governed by the migration system.

Do not leave a separate initialization path that can silently create missing
columns or tables and make a damaged database appear valid.

Existing initialization helpers may be reused by the Version 1 migration when
that remains clear and safe.

All current tests and normal application behavior must continue to work.

Logging and errors

Log:

- The database version detected at startup
- Every migration beginning
- Every migration successfully completed
- Adoption of a compatible unversioned database
- Migration or compatibility failures

Do not log sensitive customer data.

Use the existing customer-facing error-handling system rather than exposing raw
SQLite tracebacks in the interface.

Data preservation

Explicitly preserve:

- Business Defaults
- Labor defaults
- Events
- Event identity
- Event Scenarios
- Scenario assumptions
- Scenario names
- Ordered employee labor entries
- Ordered miscellaneous-cost entries
- All identifiers and relationships
- Created and modified timestamps

Do not delete, reset, or manually replace data/app.db.

Tests must never use or modify the real customer database.

Testing

Add focused automated tests covering:

- A completely fresh database reaching Version 1
- The complete current schema being created through migrations
- Migration Version 1 being recorded exactly once
- Restarting an already-current database performing no migration writes
- A compatible unversioned database being adopted as Version 1
- Existing Defaults surviving adoption unchanged
- Existing Events and Scenarios surviving adoption unchanged
- Ordered child rows surviving adoption unchanged
- Existing identifiers and timestamps remaining unchanged
- An incomplete unversioned database being rejected
- A conflicting schema being rejected
- Integrity-check or foreign-key-check failure being rejected
- Missing migrations running in numerical order
- A failed migration rolling back all schema and data changes
- A failed migration not being recorded
- Later migrations not running after an earlier failure
- An unexpected migration temporary table causing a safe failure
- A database newer than the application being rejected without modification
- Repository and route behavior working after migration
- Application restart preserving the recorded version and all customer data
- Migration logging where practical to verify

Create test fixtures or temporary database copies representing:

- Empty database
- Current unversioned Version 1-compatible database
- Already-versioned database
- Partially damaged database
- Future-version database
- Migration-failure database

Add at least one test-only future migration to prove ordered execution and
transactional rollback without changing the production schema version.

Documentation

Add concise developer documentation explaining:

- The Version 1 baseline
- How schema versions are recorded
- How to add the next migration
- Required transaction and verification behavior
- Safe table-reconstruction procedure
- Prohibition against editing an already-released migration
- Behavior for unversioned, damaged, and future-version databases

Scope

Do not implement:

- Customer-facing database backup controls
- Customer-facing restore controls
- Scheduled backups
- Backup-file management
- Desktop-specific customer-data locations
- Application update delivery
- Any unrelated schema changes

Do not add dependencies.
Do not make unrelated changes.
Do not commit.

Afterward:

1. Run focused migration, database, repository, and startup tests.
2. Run the full test suite.
3. Run git diff --check.
4. Summarize the fresh-database and upgrade behavior in plain language.
5. Explain how the current unversioned data/app.db will be adopted safely.
6. Explain how to add a future migration.
7. Report the test results.

Keep the response concise.

---------------------------------------------------------------------------------

