Next Codex Prompt

-----------------------------------------------------------

Implement the milestone:

Build travel and operating cost section.

Use Event_Analysis_Structure_v0.1.md, the current Event Analysis domain models,
and the existing Event Inputs form as the source of truth.

Add a customer-facing section titled:

“Travel and operating costs”

Add these optional fields:

- Travel cost ($)
- Parking cost ($)
- Permit cost ($)
- Generator or utility cost ($)

Behavior:

- Every field is optional.
- Travel cost is entered directly as a flat dollar amount.
- Do not add mileage or vehicle-cost-per-mile calculations.
- A blank field means no amount has been entered.
- Preserve entered values when validation elsewhere fails.

Validation:

- Values cannot be negative.
- Money values may contain no more than two decimal places.
- Invalid submissions must preserve all entered values.
- Invalid submissions must not create Event or Event Scenario records.

Scope:

- Do not copy values from Business Defaults yet.
- Do not calculate total operating cost yet.
- Do not add vendor fees, organizer commissions, fixed processing fees, or
  miscellaneous-cost entries yet.
- Do not save Events or Event Scenarios yet.
- Do not build results behavior.
- Do not add dependencies or unrelated changes.
- Do not commit.

Add tests for:

- All four fields appearing
- Every field being optional
- Valid money entries
- Negative and over-precision values being rejected
- Preserving entered values after validation failure
- No event or scenario records being saved

Afterward:

1. Run focused tests.
2. Run the full test suite.
3. Run git diff --check.
4. Summarize the visible behavior in plain language.
5. Explain how to manually test the section.
6. Report the test results.

Keep the response concise.